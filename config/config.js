const config = {
  ROUTES: {
    GITHUB: {
      GET_REPOS: {
        ENDPOINT: "/repos",
        METHOD: "GET"
      }
    }
  },
  CONSTATNS: {
    URLS: {
      GITHUB: "https://api.github.com/"
    },
    USERNAME: "loganbrownstfx"
  }
};

module.exports = config;
