const config = {
    ROUTES:{
        GITHUB:{
            GET_REPOS:{
                ENDPOINT: "/repos",
                METHOD:"GET"
            }
           
        }
    }
}

module.exports = config;